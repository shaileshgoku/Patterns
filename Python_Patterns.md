# Python Pattern Library

## Pattern 1 — Iteration Pattern

When to Use: 

Loop through elements in a collection like list, string, or range.

Pattern Structure:

for item in collection:
    # process item

Key Idea: 

Iterate through each element and perform an action.

Example:

words = ["AI", "Python", "ML"]

for word in words:
    print(word)
